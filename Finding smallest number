float main() {
   float a,b;
   printf("enter two numbers;");
   scanf("%f %f",&a,&b);
   if(a<b)
   printf("the smallest among the numbers is%f",a);
   else
   printf("the smallest among the number is%f",b);
  
    return 0;
}
